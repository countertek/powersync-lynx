# Issue #18 comment (web download regression) — paste if Issues API 403s

FM-PS-LYNX-008 **web A↔B download** is open as a PR: host-fetch `/sync/stream` was dropping the live NDJSON `Response.body` whenever `GlobalEventEmitter` existed. Ticket E's `PrimJSLynxHost` finds bare `lynx` on lynx-bg, so web always had an emitter; JS then waited on native `onData` that never arrives. Upload / Connector JSON was fine. Fix: identifier fetch keeps a usable body; `streamingId` still uses GlobalEventEmitter (native-http #21 untouched). Also: `NativeModules` is probed as a host bag (not assigned as `NativeModulesHost`) so rspeedy TS2559 is gone. Test: host-fetch fetchStream applies NDJSON while an emitter is present. Stacks on D+G (#28).
